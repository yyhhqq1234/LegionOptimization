# PHASE-00：E0 口径确认 + E1 交叉评审（文档阶段）

## 1. 阶段目标

关闭全部 P0 门禁（P0-1/P0-2 已落盘，本阶段做校验），执行 E1 t1↔t4 交叉评审，
将规格从 v0.1.1 冻结为 **v1.0**。本阶段结束前，V09+ 仍保持冻结。

## 2. 前置依赖

- `specs/SYSTEM_SPEC_DRAFT_v1.0.md` v1.0、开题大纲、围栏 v1.0 已就位（P0 交付物）
- 无需管理员、无需 B 机、无需任何真实下发

## 3. 详细步骤

### Step 0-1：P0 落盘校验（0.5 天）
1. 全文 grep 工作负载命名残留：确认 `| m0`–`| m7` 表格行、`m4/m5` prose 已全部改为 `W` 前缀，
   系统模块 `M1–M8` 保持大写未被误改（重点查 Reward 权重表、Burst 触发条件、F2 地板）。
2. 确认开题大纲 §0.2 为 G0–G4 + DC 正交复测行（无 G5 档定义），§0.3 含 W0–W7 消歧注。
3. 确认 G4 天花板为 95℃ 且 §8 含 TJMax 运行时探测条款与锁教训条款。
4. 任一项缺失 → 直接改文档，不开新流程。

### Step 0-2：E1 交叉评审表（1 天）
逐项打勾，任一项为“否”则对应 V 格保持冻结：

| # | 检查项 | 来源 | 结果 |
|---|--------|------|------|
| E1-1 | State48 必备字段存在性：CPU package/最热核/GPU 温度（滤波前后）、AC/DC、电量%、前台类别、帧统计、burst 余量、档位、风扇观测 | t4 §7.1 ↔ t1 §3 | ✅ 通过（s18/s19/s15/s2/s3/s27/s24–26/s33/s0–s1/s30–32/s23 全在§3；滤波前后双列见 t4§1.1） |
| E1-2 | Reward 含三惩罚项：温度越线 / burst 违规（P1-2 新增 `−w_burst·B_viol`，初值 0.5）/ 流畅退化 | t4 §7.2 ↔ t1 §5.1 | ✅ 通过（本阶段已增补 `−w_burst·B_viol` + B_viol 定义） |
| E1-3 | Burst 以 t4 分档表为准，t1 通用值已作废（P1-1） | t1 §6.4 ↔ t4 §3.2 | ✅ 通过（§6.4 通用 C/r 已作废，指向 t4 分档表） |
| E1-4 | 温度阈值口径：G4 95 = L1 线，距 L2 3K；DC −2℃；TJMax 相对偏移规则 | t1 §6.1/§8 ↔ t4 §1.2 | ✅ 通过（G4=95 落盘；TJMax 105 确认 + 运行时探测条款） |
| E1-5 | 否决码与 BlackBox 字段两边对齐（TEMP/SMOOTH/BURST_EMPTY/AC_LOST/MANUAL_PIN + SHIELD_PENALTY） | t1 §2.1 ↔ t4 §5.5 | ✅ 通过（五码 + SHIELD_PENALTY 两边齐备；shield_verdict 回写见 t4§5.5） |
| E1-6 | SLO 阈值冻结为 v1.0（S/D/F/A 四组 + burn 双轨） | t4 §6 | ✅ 通过（围栏头注 v1.0 冻结） |

### Step 0-3：发版（0.5 天）
1. 规格头版本改为 v1.0，三表（包络/Reward/阈值）注明冻结日期。
2. `git commit + tag phase-00-done + push`。

## 4. 阶段验收方法

- [x] `grep` 无工作负载小写残留（M1–M8 模块引用除外，已人工复核；另顺手修 thesis G5 残留 5 处 + 正交视图补 G0 行 2 机×5档×3基线=30 格）
- [x] E1-1–E1-6 全勾（见上表；执行中发现的 P1-1/P1-2 缺口已当场增补，无降级项）
- [x] `git tag phase-00-done` 存在且已推远端（Step 0-3 执行后勾；2026-09-16 复核：本地 `a118da2→535081a`，远端 `ls-remote` 同哈希，工作树干净）
- [x] 验收人签字（用户本人）：2026-09-16 用户回复"签/直接继续"——V09+ 排期资格解锁；G4 真实下发仍锁定（待 E5）

## 5. 产物清单

- `specs/SYSTEM_SPEC_DRAFT_v1.0.md`（已更名冻结）
- 本文件 E1 表格全勾版本（commit 内）

## 6. 风险与回退（执行记录补记）

- 本阶段纯文档，无运行风险。评审不通过 → 缺陷回 t1/t4 修订，不阻塞 P1/P2 的只读探针步骤。
- **遗留观察（非阻塞，转 P2）**：V23 行仍写 `automation.template` 导入冒烟 + `switch_log无ERROR`，
  两者均为已删 legacy 产物；B1 烟囱含义待 P2 结合 LKG-staging 恢复方式重写，不影响 P0 关闭。
